CREATE OR REPLACE DATABASE labs_db_basics '';

ALTER DATABASE labs_db_basics
  CHARSET DEFAULT;

ALTER DATABASE labs_db_basics
  QUERY SIMPLIFICATION OFF
  CACHE ON (
    MAINTAINERPERIOD 21600
    TIMETOLIVE 3600
    DATASOURCE DEFAULT
  )
  MEMORYCONFIG (
    SWAP ON (
      SWAPSIZE 50
      SWAPBLOCKSIZE 200
    )
    MAXRESULTSIZE 10
  );

